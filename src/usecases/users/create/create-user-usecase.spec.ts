describe('CreateUserUseCase', () => {
  it('should send a request to create an user in API and returns the created user', async () => {
    const createdUser = '1'
    const anyUser = '1'
    expect(createdUser).toBe(anyUser)
  })
})
